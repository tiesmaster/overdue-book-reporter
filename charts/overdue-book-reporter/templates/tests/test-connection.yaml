apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "overdue-book-reporter.fullname" . }}-test-connection"
  labels:
    {{- include "overdue-book-reporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "overdue-book-reporter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Service
metadata:
  name: {{ include "overdue-book-reporter.fullname" . }}
  labels:
    {{- include "overdue-book-reporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "overdue-book-reporter.selectorLabels" . | nindent 4 }}
